---
description: Live Sunrise custom-character state, albedo path, and launch discipline
alwaysApply: true
---

# Destiny2ProjectSunrise — live memory

Read `Sunrise/HANDOFF.md` before any package write. Root `HANDOFF.md` is stale (`_18`); ignore it.
Durable status: `Sunrise/docs/CUSTOM_CHARACTER.md`.

## Hard stops

- Upstream Sunrise **is merged** (2026-08-22). Do not `--undo`. Do not reopen geometry, skinning, or the tangent frame (`_22` works).
- Do **not** run `bind_material_textures.py` (tag rewrite / array append hangs character select).
- Do **not** run `assign_split_materials.py` (0691 world VS vanished four parts).
- Do **not** run `assign_armor_vs.py` (off-chest C62/C5B vanished four parts the same way).
- Do **not** steal a material that is not already on the chest model.
- Do **not** paint dye tiles (t3–t8) or sandbox 2048 sweeps. Dyes cannot carry unique atlases.
- Do **not** overwrite `0x80B3611D` with a flat colour.
- Do **not** saturation-boost the packed atlas (look-alike probe: white pants, no green).
- Do **not** retry PS `0x81531CBE` or pack another 512.
- Do **not** start another magenta/green/yellow hook that guesses RT1/RT2. The live dye PS is dumped.
- Do **not** encode `TEXCOORD2` as the world normal (v16: select dark, world a hole).
- Do **not** sample Destiny `t2` for material (v17: Scatterhorn designs on the body).
- Do **not** `--restore` `20260822-150046` (puts tiled UVs back).
- Do **not** `--fingers` / write bones 34–71 on the **chest** (needled hands to the feet).
- Do **not** AABB-fit the hands. Do **not** use the glove 0.45 AABB. Hands stay on the gauntlet draw with the **chest bind frame**.
- One launch is scarce. Combine distinguishable tests. Fill `dump/request.txt` (cap 1024) on the same trip.
- Destiny must be closed to write packages or overwrite the DLL.

## Where we are

**Character select = the Blender character** (v12, user 2026-08-22). Geometry + five-part split + unique GLB albedos.

**Live / restore = `20260822-235602.json`** (92 layers, **hands-on-gauntlets confirmed**). Live also has `globals_06dc_7` + `globals_03ed_6` (race-glove blank, not confirmed). Prior v23 wrists-only: `20260822-225414`. Tiled `0698_25` is in `packages\_reverted_uv_tiles\`. Do not `--restore` 150046.

**Sunrise DLL is hook v18** plus a 6th exact part `(0, 7902)` for gauntlet hands. v23 spine/unweld confirmed. `--fingers` is **closed**. Hands-on-gauntlets is **the floor** (user: no needles, sword grip). Do not sample Destiny t2. Do not encode TEXCOORD2. Full inventory: `Sunrise/docs/CUSTOM_CHARACTER.md`.

## Next

First-person hand-skip is **reverted** — `(0, 7902)` stays visible. FP/inspect can still spaghetti (unposed 40–71); do not hide them again. Next custom GLB goes through `Sunrise/bring_guardian.ps1` (retarget / cut / `custom_parts.txt` / `--hands-on-gauntlets`). Race-glove probe live: blanked `0x815B9521` + `0x80FDBE41`. Do **not** blank UI `0x80EFC649`. Hang / needles / leftover leather still there = restore `20260822-235602`. Do not `--fingers`. Do not AABB-fit.

## Restore

`python known_good.py --check` / `--restore` (newest snapshot = `20260822-235602`).

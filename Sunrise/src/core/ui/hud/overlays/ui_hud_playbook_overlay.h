#pragma once

namespace sunrise::core::ui::hud::overlays::playbook {

/** Draws the most recently reached roteiro step inside the overlay window the stack started. */
void draw() noexcept;

} // namespace sunrise::core::ui::hud::overlays::playbook
